# ============================================
# RAILWAY ENVIRONMENT VARIABLES
# ============================================
# Copy these to Railway Dashboard > Variables
# Railway automatically provides DATABASE_URL
# ============================================

# Django Secret Key (REQUIRED - Generate new for production!)
# Generate: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-production-secret-key-here-change-this

# Debug Mode (IMPORTANT: Set to False in production!)
DEBUG=False

# Allowed Hosts (Railway domains are auto-added)
ALLOWED_HOSTS=your-domain.com,www.your-domain.com

# Site URL
SITE_URL=https://your-app.railway.app

# Language & Timezone
LANGUAGE_CODE=el-gr
TIME_ZONE=Europe/Athens

# ============================================
# EMAIL CONFIGURATION (Optional but Recommended)
# ============================================
# For reminders and 2FA

# Option 1: Gmail (Development/Testing)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password
DEFAULT_FROM_EMAIL=CRM Πολιτικό <your-email@gmail.com>

# Option 2: SendGrid (Production Recommended)
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=apikey
# EMAIL_HOST_PASSWORD=your-sendgrid-api-key
# DEFAULT_FROM_EMAIL=CRM Πολιτικό <noreply@yourdomain.com>

# ============================================
# SECURITY SETTINGS (Production)
# ============================================
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True
SECURE_HSTS_SECONDS=31536000

# ============================================
# NOTES
# ============================================
# DATABASE_URL is automatically provided by Railway
# No need to set database variables manually!
#
# To add these variables in Railway:
# 1. Go to your project dashboard
# 2. Click on "Variables" tab
# 3. Add each variable one by one
# 4. Click "Deploy" to apply changes
